package weirdkey.runtime;

public final class InstallationContext extends RuntimeContext {
    InstallationContext(GameContext game, Object installationState) {
        super(game, installationState);
    }
}
